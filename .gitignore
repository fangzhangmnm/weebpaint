# 人类写的 journal，不入库（proposal 明确要求）
journal/
journals/

# machine generated reports
ai-docs/reports/

# 抛弃型原型 / 草稿
tmp/

# OS
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/

# 工具产物
node_modules/
*.log
.cache/
debug/

# v122：dist/main-<hash>.mjs 是 build 产物**入** git（跟 v121 之前一样 naive 结构，
# 源树就只多 dist/ 这一个新东西）。GH Actions 的 deploy.yml 只是把 prod + main 两
# 个分支的现成 dist 组合上 Pages，不重 build。
#
# v122 r2: tools/esbuild 跨 OS 不通用且 10MB；build.sh 没找到会自动 curl 一份。
# 重命名自 vendor/esbuild：tools/ = 构建工具（gitignored）；vendor/ = 运行时 lib（commit 进 git）。
tools/esbuild/

# v122 build 中间产物
dist/main-tmp.mjs
dist/main-tmp.mjs.map
# Claude Code session 区（worktree / 本地设置），不入库
.claude/

# GL smoke：esbuild 生成的 bundle（test/gl-smoke/harness.ts → harness.js）
test/gl-smoke/harness.js
test/gl-smoke/preview.js

# C9 家族组件 vendor 导出件（scripts/gen-component.sh 产物；兄弟项目拷走用，本仓不跑它）
dist-components/

# SVG 图标工作台（本地预览工具，不入库）
workbench/
node_modules
journals/

# private 口袋（毕业分拣制 2026-08-20）：AI 可写的私域，不入公开仓；语义同 journal 之于人类区。
# 惰性创建——第一次真要写私密内容才建仓（届时自己 git init + OneDrive bare 远端）。
private/
dist/weebpaint-standalone.html
