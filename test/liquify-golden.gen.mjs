// 液化 golden 生成器 —— **已封存**（created 2026-09-19 by Claude Fable 5.1）。
//   fixture test/fixtures/liquify-golden.json 于 2026-09-19 用旧 CPU LiquifyEngine（v0.14.19，git e4e5aed）录定，之后旧引擎已被 GPU 区域程序版
//   替换（同一文件名 src/plugins/liquify-engine.ts，签名不同）。锚的意义 = 不许用新引擎重录来让测试变绿。
//   真要重录：`git show e4e5aed:test/liquify-golden.gen.mjs` + 从 e4e5aed 复活旧引擎到临时路径，并在 commit message 说明为什么改锚。
console.error("liquify-golden.gen.mjs is sealed: the golden was recorded from the CPU engine at git e4e5aed; the CPU engine is gone. See file header.");
process.exit(1);
